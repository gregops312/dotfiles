# Ruby

ruby_setup () {
  echo "mkdir dirname"
  echo "dirname = dirname/.ruby-gemset"
  echo "cd dirname"
  echo "source 'https://rubygems.org'\n\ngem 'byebug' = Gemfile "
  echo "gem install bundler --no-ri --no-rdoc"
  echo "bundle install"
}

